import { requireAdmin } from "@/lib/auth"; import { prisma } from "@/lib/prisma"; import { generateOperationId } from "@/lib/pos2/operationId"; import { adjustAction, configureUnitAction, countAction, receiveAction, transferAction } from "./actions";
const field="rounded border border-outline-variant bg-background px-2 py-1";
export default async function InventoryV2Page({searchParams}:{searchParams:Promise<{branchId?:string}>}){await requireAdmin();const q=await searchParams;const branches=await prisma.branch.findMany({where:{active:true},orderBy:{name:"asc"}});const branchId=q.branchId??branches[0]?.id;const [products,balances,movements]=await Promise.all([prisma.inventoryProduct.findMany({where:{isActive:true},orderBy:{name:"asc"}}),prisma.inventoryBalance.findMany({where:{branchId},include:{inventoryProduct:true},orderBy:{inventoryProduct:{name:"asc"}}}),prisma.inventoryMovement.findMany({where:{branchId},include:{inventoryProduct:true},orderBy:{createdAt:"desc"},take:100})]);const configured=products.filter(p=>p.inventoryBaseUnit);const productSelect=(name:string)=><select className={field} name={name}>{configured.map(p=><option key={p.id} value={p.id}>{p.code} · {p.name} [{p.inventoryBaseUnit}]</option>)}</select>;return <main className="mx-auto max-w-7xl space-y-6 p-6"><header><h1 className="text-2xl font-bold">Inventory Ledger V2</h1><p className="text-sm">InventoryProduct es el único artículo físico. Los balances no se editan directamente.</p></header><form><select className={field} name="branchId" defaultValue={branchId}>{branches.map(b=><option key={b.id} value={b.id}>{b.name}</option>)}</select><button className="ml-2 underline">Ver sucursal</button></form>
<details className="rounded border p-3"><summary>Configurar unidad V2</summary>{products.filter(p=>!p.inventoryBaseUnit).map(p=><form key={p.id} action={configureUnitAction} className="flex gap-2 p-1"><input type="hidden" name="inventoryProductId" value={p.id}/><span>{p.code} · {p.name} (legacy: {p.unit})</span><select name="unit"><option>UNIT</option><option>ML</option></select><button className="underline">Configurar</button></form>)}</details>
<section className="grid gap-3 lg:grid-cols-4"><form action={receiveAction} className="space-y-2 rounded border p-3"><b>Recibir</b><input type="hidden" name="operationId" value={generateOperationId()}/><input type="hidden" name="branchId" value={branchId}/>{productSelect("inventoryProductId")}<input className={field} name="quantity" required/><select name="unit"><option>UNIT</option><option>ML</option></select><input className={field} name="sourceId" required placeholder="Documento"/><input className={field} name="reason" required placeholder="Motivo"/><button>Registrar</button></form><form action={adjustAction} className="space-y-2 rounded border p-3"><b>Ajustar</b><input type="hidden" name="operationId" value={generateOperationId()}/><input type="hidden" name="branchId" value={branchId}/>{productSelect("inventoryProductId")}<input className={field} name="delta" required placeholder="+/-"/><select name="unit"><option>UNIT</option><option>ML</option></select><input className={field} name="reason" required/><button>Aplicar</button></form><form action={countAction} className="space-y-2 rounded border p-3"><b>Conteo</b><input type="hidden" name="operationId" value={generateOperationId()}/><input type="hidden" name="branchId" value={branchId}/>{productSelect("inventoryProductId")}<input className={field} name="declaredQuantity" required/><select name="unit"><option>UNIT</option><option>ML</option></select><input className={field} name="notes"/><button>Declarar</button></form><form action={transferAction} className="space-y-2 rounded border p-3"><b>Transferir</b><input type="hidden" name="operationId" value={generateOperationId()}/><input type="hidden" name="fromBranchId" value={branchId}/><select className={field} name="toBranchId">{branches.filter(b=>b.id!==branchId).map(b=><option key={b.id} value={b.id}>{b.name}</option>)}</select>{productSelect("inventoryProductId")}<input className={field} name="quantity" required/><select name="unit"><option>UNIT</option><option>ML</option></select><input className={field} name="reason" required/><button>Transferir</button></form></section>
<section><h2 className="font-semibold">Balances</h2>{balances.map(b=><div key={b.id} className="border-b p-2">{b.inventoryProduct.code} · {b.inventoryProduct.name}: <b>{b.quantity.toString()} {b.unit}</b> · v{b.version}</div>)}</section><section><h2 className="font-semibold">Últimos movimientos</h2>{movements.map(m=><div key={m.id} className="border-b p-2 text-sm">{m.createdAt.toLocaleString("es-MX")} · {m.inventoryProduct.name} · {m.movementType} · {m.quantityDelta.toString()} · {m.balanceBefore.toString()} → {m.balanceAfter.toString()} · {m.sourceType}/{m.sourceId}</div>)}</section></main>}
